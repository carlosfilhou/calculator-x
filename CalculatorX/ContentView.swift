import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            HStack {
                Button("7") {
                    
                }
                .background(Color.black)
            }
        }
    }
}

#Preview {
    ContentView()
}
